var configHelperPath = '../../common/confighelper'
  , sinon = require('sinon')
  , chai = require('chai')
  , expect = chai.expect();

describe('ConfigHelper', function() {
  var sut; 
  

  describe('get NODE_ENV', function () {
    describe('without env value', function() {
      it('should be development', function() {

        //sut = require(configHelperPath);      
        //expect(sut.NODE_ENV).to.equal('development');
        
      });
    })
  });



});